import { InjectEnabled } from './injectable';

/**
 * DIContainer is a utility for layered architecture based apps
 * that allows a decoupling of dependencies and components that
 * depend on them.
 */
export class DIContainer {
  lookUp(lookUpToken: InjectEnabled) {
    return null;
  }
}
